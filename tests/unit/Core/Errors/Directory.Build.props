<Project>

    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

    <PropertyGroup>
        <RootNamespace>Paraminter.Associating.CSharp.Attributes.Constructor.Phrike.Errors</RootNamespace>
    </PropertyGroup>
    
</Project>